version: '3'
services:
  web:
    build: .
    ports:
      - 8000:8000
    depends_on:
      - db
  db:
    image: postgres
    environment:
      POSTGRES_USER: lyfenko
      POSTGRES_PASSWORD: KBATYRJ2
      POSTGRES_DB: database
